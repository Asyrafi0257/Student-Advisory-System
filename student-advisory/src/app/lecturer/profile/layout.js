export const metadata = {
    title: "Profile",
    describe: "This page for mentor profile to make change their information"
}

export default function Profile({ children }) {
    return (
        <div>{children}</div>
    )
}