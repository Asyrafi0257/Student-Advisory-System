export const metadata = {
    title: "Submit Session",
    describe: "This page for admin to submit session after they do session with mentee"
}

export default function SubmitLayout({ children }) {
    return (
        <div>
            {children}
        </div>
    )
}