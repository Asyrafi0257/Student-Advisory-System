export const metadata = {
    title: "Session",
    describe: "This session page for mentor to create session for student"
}

export default function Session({ children }) {
    return (
        <div>{children}</div>
    )
}