export const metadata = {
    title: "View Session",
    describe: "This page mentor can see their session after mentor submitted session to admin"
}

export default function viewPage({ children }) {
    return (
        <div>{children}</div>
    )
}