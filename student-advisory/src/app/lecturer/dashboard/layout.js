export const metadata = {
    title: "Dashboard",
    describe: "This dashboard for display information about mentee and mentor"
}

export default function Dashboard({ children }) {
    return (
        <div>
            {children}
        </div>
    )
}