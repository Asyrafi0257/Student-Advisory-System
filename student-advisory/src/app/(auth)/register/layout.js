export const metadata = {
    title: "Register",
    describe: "This page for register to student and lecture"
}

export default function Register({ children }) {
    return (
        <div>{children}</div>
    )
}