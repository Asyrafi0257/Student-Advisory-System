export const metadata = {
    title : "Login",
    description : "Login page for Student, Lecture and Admin"
}
export default function loginLayout({children}){
    return(
        <div>{children}</div>
    )
}