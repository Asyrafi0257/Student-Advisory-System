export const metadata = {
    title: "Report",
    describe: "This is page for admin to view mentor report about mentor's session with mentee"
};

export default function ReportLayout({ children }) {
    return (
        <div>
            {children}
        </div>
    )
}