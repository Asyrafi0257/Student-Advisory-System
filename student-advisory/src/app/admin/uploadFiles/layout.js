export const metadata = {
    title : "Upload Student",
    description : "Admin upload data student to save student's information"
}

export default function LayoutUploadFile({children}){
    return (
        <div>{children}</div>
    )
}