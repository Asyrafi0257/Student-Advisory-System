export const metadata = {
    title:"Upload Mentor",
    default : "This page allow admin to upload data mentor either active or non-active"
}

export default function LayoutUploadMentor({children}){
    return (
        <div>{children}</div>
    )
}