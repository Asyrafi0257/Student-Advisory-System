const metadata = {
    title: "Profile Admin",
    describe: "This page admin can edit thier profile at profile page"
};

export default function Profile({ children }) {
    return (
        <div>{children}</div>
    )
}