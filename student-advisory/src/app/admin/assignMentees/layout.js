export const metadata = {
    title:"Assigned Mentees",
    describe: "Admin can assign mentee to mentor with drag and drop"
}

export default function LayoutAssignedMentee({children}){
    return(
        <div>{children}</div>
    )
}