export const metadata = {
    title: "History",
    describe: "This page for admin to view history lecture retired with their mentee"
}

export default function HistoryLayout({ children }) {
    return (
        <div>{children}</div>
    )
}