export const metadata = {
    title: "List Mentor",
    describe: "Admin can see all mentor active and deactive"
}

export default function LayoutListMentor({children}){
    return(
        <div>{children}</div>
    )
}