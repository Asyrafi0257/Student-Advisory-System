export const metadata = {
    title: "View Assignment",
    describe: "Admin can see the mentee to mentor at view assignment"
}

export default function ViewAssign({ children }) {
    return (
        <div>{children}</div>
    )
}