export const metadata = {
    title: "List Student",
    description : "Admin can see list student either data already exist or upload it"
}

export default function LayoutListStudent({children}){
    return(
        <div>{children}</div>
    )
}