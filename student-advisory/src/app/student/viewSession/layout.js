export const metadata = {
    title: "View Session",
    describe: "This page for student who can see their session that created by their mentor"
}

export default function Session({ children }) {
    return (
        <div>
            {children}
        </div>
    )
}