export const metadata = {
    title: "Student Profile",
    describe: "This page for studet who make update their information"
}

export default function LayoutProfile({ children }) {
    return (
        <div>{children}</div>
    )
}