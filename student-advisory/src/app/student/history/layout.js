export const metadata = {
    title: "History",
    describe: "This page for student to view history lecture retired"
}

export default function HistoryMentorLayout({ children }) {
    return (
        <div>{children}</div>
    )
}